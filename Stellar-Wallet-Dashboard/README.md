# Stellar Wallet Dashboard (local scaffold)

Simple dashboard to generate/load wallets, view balances, and submit payments.

Open `index.html` to run the static demo or run the backend to power API calls.
